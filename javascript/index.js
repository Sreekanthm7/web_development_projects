const fruits = ["Orange", "Apple", "Mango", "Pine apple", "Banana"]
fruits.reverse()

fruits.map((item) => {
  return item
})

const animals = ["dogss", "cats", "elephants", "tiger", "cheeta"]

animals.push("donkeys")

animals.pop("dogs")

animals.length
