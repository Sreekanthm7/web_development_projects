const fruits = ["Orange", "Apple", "Mango", "Pine apple", "Banana"]
fruits.reverse()

fruits.map((item) => {
  return item
})

const animals = ["dog", "cat", "elephant", "tiger", "cheeta"]

animals.push("donkeys")

animals.pop("dogs")

animals.length
